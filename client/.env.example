# Local development (also see .env.development)
REACT_APP_API_URL=http://localhost:5000/api

# Render static site (PG-client only): set in Render dashboard before build
# REACT_APP_API_URL=https://YOUR-SERVICE-NAME.onrender.com/api

# Vercel: optional — leave unset to use /api proxy in vercel.json
