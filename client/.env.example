# Local development (also see .env.development)
REACT_APP_API_URL=http://localhost:5000/api

# Production backend on Vercel (must end with /api)
# REACT_APP_API_URL=https://pg-backend-lyart.vercel.app/api

# Frontend Vercel project: set the line above OR leave unset to use /api proxy in vercel.json
